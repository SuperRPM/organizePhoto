{
  "name": "npm",
  "version": "1.0.0",
  "description": "test project",
  "main": "bin/photo.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app",
    "rpm": "node app"
  },
  "dependencies": {
    "nodemon": "^2.0.12"
  },
  "bin": {  
    "tran": "./bin/photo.js"  
  }
}
